language: node_js
sudo: false
dist: trusty
node_js: stable
addons:
  firefox: latest
  chrome: stable
cache:
  directories:
  - node_modules
before_script:
- npm install -g bower gulp-cli@1
- bower install
script:
- xvfb-run wct